# 项目协作规则

## 开始工作

- 先读 `README.md`、`docs/CONVENTIONS.md` 和 `docs/PROGRESS.md`，再核对实际文件。
- 开始与交接时说明本轮对应的目标编号；目标索引在 `docs/PROGRESS.md`，编号与对话命名规则以 `docs/CONVENTIONS.md` 为准。
- 以用户当前会话中明确的要求为准；用户调整长期方向时，同步修正文档。
- 区分已经实现的事实、建议方案和待验证事项。不得把计划描述成已完成的功能。

## 学习与沟通

- 用户有 Java 基础，但没有完整项目开发经验，正在学习 Kotlin、KMP 和 Compose。默认使用中文解释，代码标识符使用英文；首次出现构建、依赖、模块等术语时先说明用途。
- 按本轮要求决定讲解或实现；引入新概念时说明用途，并在有帮助时对照 Java。
- 每次实现围绕一个可运行、可验证的小目标，保持代码便于阅读和修改。
- 用户要求实现时完成该目标，不把课后练习或例行确认作为前提。

## 技术与范围

- 已确定路线为 Kotlin + KMP + Compose Multiplatform，先做 Windows 桌面。
- 共享部分放在 `commonMain`；桌面入口及 JVM 专用调用放在 `desktopMain`。目录尚未创建时，按当前阶段逐步建立。
- 不在共享代码中使用 `java.*`、`javax.*` 或 Android 专用 API。
- 界面、状态处理和数据获取职责分开；UI 组件不直接发网络请求或读写文件。
- 仅在当前功能需要时增加依赖、模块或抽象。技术版本固定，并记录有影响的选择理由。
- 具体代码和界面标准见 `docs/CONVENTIONS.md`，不要在多处维护不同版本的规则。

## 验证与交接

- 运行与本次变更相关且实际存在的检查；文档变更检查内容和链接即可。
- 构建、测试、运行和人工检查分别报告，不把其中一项通过当成全部通过。
- 不能执行的检查标为未验证，写清可观察到的原因；不虚构成功结果。
- 完成功能或阶段后，按原目标编号更新 `docs/PROGRESS.md` 中的现状、验证结果和下一步。
- 改变运行方式时更新 `README.md`；改变长期约定时更新 `docs/CONVENTIONS.md`。
- 交接文档保持简短，保留当前有效信息；不复制整段聊天、原始日志或凭证。
